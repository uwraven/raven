// #include <Estimator.h>
// #include <Controller.h>
// #include <Allocator.h>
// #include <Monitor.h>

class FlightController
{
public:
    FlightController();

    bool init();
    bool calibrate();
    void main();

    // Estimator estimator;
    // Controller controller;
    // Allocator allocator;
    // Monitor monitor;

private:
};